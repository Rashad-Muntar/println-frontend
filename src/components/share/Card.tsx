import React from 'react'
import { Text, View, Button } from 'react-native'

interface Props {
    title: string,
    subtext?: string
    onPress?: () => void
    icon?: any
} 
const Card = () => {
  return (
    <View>
        <Button title=''/>
        Btn
      </View>
  )
}

export default Btn