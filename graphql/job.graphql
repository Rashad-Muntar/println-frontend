mutation createJob($input: NewJob!) {
  createJob(input: $input) {
    Id
    onGoing
    completed
    description
    file
  }
}

mutation processFile($url: String!){
  processFile(url: $url){
    url
    price
  }
}
